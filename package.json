{
  "name": "Lab-01",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "pocketbase": "^0.26.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-easy-crop": "^5.5.3",
    "react-router-dom": "^6.26.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "watch-icons": "chokidar 'src/assets/icons/*' -c 'cd src/data && node IconGener.js'",
    "prestart": "node src/data/IconGener.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "nodemon": "^3.1.4",
    "npm-run-all": "^4.1.5",
    "react-scripts": "^5.0.1"
  }
}
